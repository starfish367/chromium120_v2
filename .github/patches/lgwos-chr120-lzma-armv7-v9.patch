diff --git a/src/third_party/lzma_sdk/C/CpuArch.c b/src/third_party/lzma_sdk/C/CpuArch.c
--- a/src/third_party/lzma_sdk/C/CpuArch.c
+++ b/src/third_party/lzma_sdk/C/CpuArch.c
@@ -436,4 +436,13 @@
 #elif defined(MY_CPU_ARM)
-  #define MY_HWCAP_CHECK_FUNC(name) \
-  BoolInt CPU_IsSupported_ ## name() { return (getauxval(AT_HWCAP2) & (HWCAP2_ ## name)) ? 1 : 0; }
+  #if defined(__ARM_ARCH) && (__ARM_ARCH >= 8) && defined(AT_HWCAP2) && \
+      defined(HWCAP2_CRC32) && defined(HWCAP2_SHA1) && \
+      defined(HWCAP2_SHA2) && defined(HWCAP2_AES)
+    #define MY_HWCAP_CHECK_FUNC(name) \
+    BoolInt CPU_IsSupported_ ## name() { return (getauxval(AT_HWCAP2) & (HWCAP2_ ## name)) ? 1 : 0; }
+  #else
+    // LGWOS: ARMv7/Cortex-A9 has NEON in AT_HWCAP, but no ARMv8 crypto-extension HWCAP2 ABI.
+    // Keep NEON detection below intact and report ARMv8-only CRC32/SHA/AES as unavailable.
+    #define MY_HWCAP_CHECK_FUNC(name) \
+    BoolInt CPU_IsSupported_ ## name() { return 0; }
+  #endif
   MY_HWCAP_CHECK_FUNC_2(NEON, NEON)

diff --git a/src/third_party/crashpad/crashpad/compat/linux/sys/mman_memfd_create.cc b/src/third_party/crashpad/crashpad/compat/linux/sys/mman_memfd_create.cc
index 0b559ec..827b707 100644
--- a/src/third_party/crashpad/crashpad/compat/linux/sys/mman_memfd_create.cc
+++ b/src/third_party/crashpad/crashpad/compat/linux/sys/mman_memfd_create.cc
@@ -18,6 +18,12 @@
 #include <sys/syscall.h>
 #include <unistd.h>
 
+#if defined(__arm__) && !defined(SYS_memfd_create)
+// LGWOS: old webOS ARM userspace headers predate memfd_create. Linux ARM EABI
+// reserves syscall 385 for memfd_create; unsupported old kernels return ENOSYS.
+#define SYS_memfd_create 385
+#endif
+
 #include "util/misc/no_cfi_icall.h"
 
 #if defined(__GLIBC__)

diff --git a/src/third_party/crashpad/crashpad/util/file/file_io_posix.cc b/src/third_party/crashpad/crashpad/util/file/file_io_posix.cc
--- a/src/third_party/crashpad/crashpad/util/file/file_io_posix.cc
+++ b/src/third_party/crashpad/crashpad/util/file/file_io_posix.cc
@@ -21,5 +21,11 @@
 #include <sys/stat.h>
 #include <unistd.h>
 
+#ifndef O_TMPFILE
+// LGWOS: the legacy webOS userspace headers predate Linux 3.11. Preserve
+// Crashpad's runtime fallback by supplying the stable Linux open(2) flag.
+#define O_TMPFILE (020000000 | O_DIRECTORY)
+#endif
+
 #include <algorithm>
 #include <limits>

diff --git a/src/third_party/crashpad/crashpad/util/linux/ptracer.cc b/src/third_party/crashpad/crashpad/util/linux/ptracer.cc
--- a/src/third_party/crashpad/crashpad/util/linux/ptracer.cc
+++ b/src/third_party/crashpad/crashpad/util/linux/ptracer.cc
@@ -17,7 +17,11 @@
 #include <errno.h>
 #include <linux/elf.h>
 #include <string.h>
 #include <sys/ptrace.h>
 #include <sys/uio.h>
 
+#ifndef PTRACE_GETREGSET
+#define PTRACE_GETREGSET static_cast<enum __ptrace_request>(0x4204)
+#endif
+
 #include "base/check_op.h"

diff --git a/src/ui/events/ozone/evdev/event_device_util.h b/src/ui/events/ozone/evdev/event_device_util.h
index 2452a5c..8dca482 100644
--- a/src/ui/events/ozone/evdev/event_device_util.h
+++ b/src/ui/events/ozone/evdev/event_device_util.h
@@ -7,6 +7,23 @@
 
 #include <limits.h>
 
+// LGWOS: the legacy Linux input UAPI headers predate these event-code names,
+// while their numeric slots are already inside the v3.10 ABI ranges.
+// Supply the stable values so Chromium's optional capability probes compile;
+// unsupported devices simply report the corresponding bit as unset.
+#ifndef INPUT_PROP_POINTING_STICK
+#define INPUT_PROP_POINTING_STICK 0x05
+#endif
+#ifndef KEY_ASSISTANT
+#define KEY_ASSISTANT 0x247
+#endif
+#ifndef SW_MUTE_DEVICE
+#define SW_MUTE_DEVICE 0x0e
+#endif
+#ifndef SW_PEN_INSERTED
+#define SW_PEN_INSERTED 0x0f
+#endif
+
 namespace ui {
 
 #define EVDEV_LONG_BITS (CHAR_BIT * sizeof(long))

diff --git a/src/ui/ozone/platform/wayland/host/wayland_buffer_backing.h b/src/ui/ozone/platform/wayland/host/wayland_buffer_backing.h
--- a/src/ui/ozone/platform/wayland/host/wayland_buffer_backing.h
+++ b/src/ui/ozone/platform/wayland/host/wayland_buffer_backing.h
@@ -13,6 +13,11 @@
 #else
 #include "drm_fourcc.h"
 #endif
+#ifndef DRM_FORMAT_INVALID
+// Stable DRM fourcc ABI: value 0 is reserved for an invalid format.
+#define DRM_FORMAT_INVALID 0
+#endif
+
 #include "ui/gfx/geometry/size.h"
 #include "ui/gfx/native_widget_types.h"
 #include "ui/ozone/platform/wayland/common/wayland_object.h"

diff --git a/src/ui/ozone/platform/wayland/host/zwp_text_input_wrapper_v1.cc b/src/ui/ozone/platform/wayland/host/zwp_text_input_wrapper_v1.cc
--- a/src/ui/ozone/platform/wayland/host/zwp_text_input_wrapper_v1.cc
+++ b/src/ui/ozone/platform/wayland/host/zwp_text_input_wrapper_v1.cc
@@ -240,13 +240,34 @@
   // so if it exceeds 16 bits, it may be broken.
   static constexpr size_t kSizeLimit = 60000;
   if (HasAdvancedSurroundingTextSupport() && text.length() > kSizeLimit) {
+#if defined(OS_WEBOS)
+    // Linux 3.10 predates memfd_create(). Use an unlinked temporary file;
+    // the Wayland request only requires an fd containing the surrounding text.
+    base::FilePath temp_dir;
+    base::FilePath temp_path;
+    if (!base::GetTempDir(&temp_dir)) {
+      LOG(ERROR) << "Failed to locate temporary directory";
+      return;
+    }
+    base::ScopedFD memfd =
+        base::CreateAndOpenFdForTemporaryFileInDir(temp_dir, &temp_path);
+    if (!memfd.is_valid()) {
+      PLOG(ERROR) << "Failed to create surrounding-text temporary file";
+      return;
+    }
+    if (!base::SetCloseOnExec(memfd.get()) || !base::DeleteFile(temp_path)) {
+      PLOG(ERROR) << "Failed to anonymize surrounding-text temporary file";
+      return;
+    }
+#else
     base::ScopedFD memfd(memfd_create("surrounding_text", MFD_CLOEXEC));
-    if (!memfd.get()) {
+    if (!memfd.is_valid()) {
       PLOG(ERROR) << "Failed to create memfd";
       return;
     }
+#endif
     if (!base::WriteFileDescriptor(memfd.get(), text)) {
-      LOG(ERROR) << "Failed to write into memfd";
+      LOG(ERROR) << "Failed to write surrounding text into shared fd";
       return;
     }
     zcr_extended_text_input_v1_set_large_surrounding_text(

diff --git a/src/ui/ozone/platform/wayland/extensions/webos/host/wayland_extensions_webos_impl.h b/src/ui/ozone/platform/wayland/extensions/webos/host/wayland_extensions_webos_impl.h
--- a/src/ui/ozone/platform/wayland/extensions/webos/host/wayland_extensions_webos_impl.h
+++ b/src/ui/ozone/platform/wayland/extensions/webos/host/wayland_extensions_webos_impl.h
@@ -59,9 +59,9 @@ class WaylandExtensionsWebosImpl : public WaylandExtensions,
       PlatformWindowDelegate* delegate,
       WaylandConnection* connection) override;
 
-#if defined(USE_NEVA_MEDIA) && defined(USE_GAV)
+#if defined(USE_NEVA_MEDIA)
   VideoWindowProviderDelegate* GetVideoWindowProviderDelegate() override;
-#endif  // defined(USE_NEVA_MEDIA) && defined(USE_GAV)
+#endif  // defined(USE_NEVA_MEDIA)
 
   // WaylandExtensionsWebos overrides
   ExtendedInputWrapper* GetExtendedInput() override;

diff --git a/src/ui/ozone/platform/wayland/extensions/webos/host/wayland_extensions_webos_impl.cc b/src/ui/ozone/platform/wayland/extensions/webos/host/wayland_extensions_webos_impl.cc
--- a/src/ui/ozone/platform/wayland/extensions/webos/host/wayland_extensions_webos_impl.cc
+++ b/src/ui/ozone/platform/wayland/extensions/webos/host/wayland_extensions_webos_impl.cc
@@ -179,12 +179,16 @@ std::unique_ptr<WaylandWindow> WaylandExtensionsWebosImpl::CreateWaylandWindow(
   return std::make_unique<WaylandWindowWebos>(delegate, connection, this);
 }
 
-#if defined(USE_NEVA_MEDIA) && defined(USE_GAV)
+#if defined(USE_NEVA_MEDIA)
 VideoWindowProviderDelegate*
 WaylandExtensionsWebosImpl::GetVideoWindowProviderDelegate() {
+#if defined(USE_GAV)
   return foreign_video_window_manager_.get();
+#else
+  return nullptr;
+#endif
 }
-#endif  // defined(USE_NEVA_MEDIA) && defined(USE_GAV)
+#endif  // defined(USE_NEVA_MEDIA)
 
 ExtendedInputWrapper* WaylandExtensionsWebosImpl::GetExtendedInput() {
   return extended_input_.get();
